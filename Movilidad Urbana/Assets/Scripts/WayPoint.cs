using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WayPoint : MonoBehaviour
{

    public Transform nexPoint;//Original
    public Transform[] waypointsPosibles;



    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        // int n =Random.Range(0,waypointsPosibles.Length);
        // GameObject nexPoint = waypointsPosibles[n];
    }
}
